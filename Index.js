// Definizione Algoritmo:
// Un'algoritmo è una sequenza di calcoli o operazioni logiche creata per elaborare un input ed ottenere un determinato output.
// L'esempio più comune è quello di paragonare un'algoritmo ad una ricetta di cucina, la quale parte da determinati ingredienti (input) per ottenere la pietanza finita (output) tramite la sua esecuzione (algoritmo).

// Definizione Variabile:
// Le variabili sono allocazioni di memoria in cui l'algorimto inserisce dati che hanno bisogno di una elaborazione intermedia, prima di poter essere utilizzati per completare il processo.
// Sempre utilizzando la ricetta culinaria come esempio, una variabile equivale ad un contenitore, in cui andremo a mettere ingredienti che hanno bisgno di una lavorazione intermedia per poter essere utilizzati, come potrebbero essere uova e farina per crare l'impasto base della ricetta.

// Undefined vs. Null:
// Quando una viariabile viene dichiarata non ha un contenuto definito (undefined), mentre null è la keyword che svuota il contenuto di una variabile.